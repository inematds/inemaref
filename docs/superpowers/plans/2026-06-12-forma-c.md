# Forma C — filme cinematográfico Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produzir a "Forma C" (filme cinematográfico dos painéis) reaproveitando os painéis textless + narrações da Forma A, dirigindo via o skill `diretor-animacao` (render pixflow), e empacotando com gancho de abertura + CTA, com saída `<serie>-c-epNN-<titulo>.mp4`.

**Architecture:** O grosso da Forma C é o skill externo `diretor-animacao` (dirigido por Claude: VÊ as imagens, decide câmera por quadro, renderiza via pixflow). O código novo no inemaref é a **cola**: (1) um **coletor** que encena os painéis textless + narrações no layout que o diretor consome (`assets/img/sNN.png` + `assets/audio/sNN.wav`) + um manifesto; (2) **naming** `<serie>-c-ep…`; (3) um **wrapper** que envolve o filme dirigido (o "miolo") com o card de abertura narrando o gancho (t=0) e o CTA inema.club, reusando `_title_clip`/`_cta_clip` do motioncomic. As Formas A/B não são tocadas.

**Tech Stack:** Python 3 (stdlib: shutil/os/json/subprocess) · ffmpeg · `diretor-animacao` (node/remotion) · `pixflow` (node/remotion/WebGL + Depth-Anything-V2, GPU). Testes: scripts `test_*.py` rodados com `python3` (como o resto do repo).

---

## Estrutura de arquivos

- `skill/motioncomic/scripts/forma_c.py` — **novo**. Coletor (`coletar_forma_c`) + naming (`forma_c_out_name`) + wrapper (`wrap_forma_c`). Um arquivo focado, sem tocar build_motion/build_travel (só importa `_title_clip`/`_cta_clip`/`W`/`H`/`FPS` deles).
- `skill/motioncomic/tests/test_forma_c.py` — **novo**. Testa coletor (encenação + manifesto + ordem), naming, e o wrapper (concat com clipes fake).
- `docs/superpowers/specs/2026-06-12-forma-c-design.md` — spec (já existe).

Nada de novo em `serie` (Forma C não é tipo do lote determinístico; é fluxo dirigido).

---

### Task 0: Validar o render pixflow/diretor (PORTÃO de viabilidade)

Não é TDD — é o gate de risco. Confirma que pixflow renderiza neste ambiente ANTES de construir a cola.

**Files:** nenhum (validação).

- [ ] **Step 1: Render do demo do pixflow**

Run:
```bash
cd ~/projetos/pixflow/skill && node cli/pixflow-motion.mjs render examples/demo.movie.yaml /tmp/pixflow_demo.mp4 --fast 2>&1 | tail -20
```
Expected: termina sem erro e cria `/tmp/pixflow_demo.mp4`.

- [ ] **Step 2: Conferir o MP4**

Run:
```bash
ffprobe -v error -show_entries format=duration,size -of default=nk=1 /tmp/pixflow_demo.mp4
```
Expected: duração > 0 e size > 0 (arquivo de vídeo válido).

- [ ] **Step 3: Se falhar — diagnosticar, não prosseguir**

Se o Step 1 falhar (depth model não baixa, remotion/chromium quebra, GPU): registrar o erro e **parar o plano aqui** (BLOCKED) — a Forma C depende deste motor. Conferir `~/projetos/diretor-animacao/DICAS-CORRECOES.md` e o README do pixflow. NÃO seguir pras tasks de cola sem o render comprovado.

- [ ] **Step 4: Sem commit** (nada de código nesta task).

---

### Task 1: Coletor — encena painéis textless + narrações p/ o diretor

**Files:**
- Create: `skill/motioncomic/scripts/forma_c.py`
- Test: `skill/motioncomic/tests/test_forma_c.py`

- [ ] **Step 1: Write the failing test**

```python
# skill/motioncomic/tests/test_forma_c.py
import os, sys, json, shutil
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "scripts"))
import forma_c

def _fake_assets(d):
    """Cria assets/pNNqN.png + .wav vazios p/ 2 paginas x 6 paineis."""
    a = os.path.join(d, "assets"); os.makedirs(a, exist_ok=True)
    for pn in (1, 2):
        for q in range(1, 7):
            open(os.path.join(a, f"p{pn:02d}q{q}.png"), "wb").close()
            open(os.path.join(a, f"p{pn:02d}q{q}.wav"), "wb").close()
    return a

ROT = {
    "id": "demo-ep01", "n": 1, "titulo": "Um Titulo",
    "abertura": "Gancho de abertura.", "subtitulo": "subt",
    "paginas": [
        {"n": 1, "titulo": "P1", "paineis": [{"prompt": f"cena {i}"} for i in range(1, 7)]},
        {"n": 2, "titulo": "P2", "paineis": [{"prompt": f"cena {i}"} for i in range(7, 13)]},
    ],
}

def test_coletor_encena_em_ordem(tmp="/tmp/_forma_c_col"):
    shutil.rmtree(tmp, ignore_errors=True)
    src = _fake_assets(os.path.join(tmp, "src"))
    stage = os.path.join(tmp, "stage")
    man = forma_c.coletar_forma_c(ROT, src, stage)
    # 12 paineis -> s01..s12, encenados em img/ e audio/
    assert len(man["imagens"]) == 12 and len(man["narracoes"]) == 12
    assert os.path.exists(os.path.join(stage, "assets", "img", "s01.png"))
    assert os.path.exists(os.path.join(stage, "assets", "img", "s12.png"))
    assert os.path.exists(os.path.join(stage, "assets", "audio", "s01.wav"))
    # ordem de leitura: p01q1..p01q6, p02q1..p02q6
    assert man["narracoes"][0].endswith("s01.wav")
    assert man["narracoes"][6].endswith("s07.wav")   # 1o painel da pagina 2
    # manifesto persistido + meta
    saved = json.load(open(os.path.join(stage, "forma-c.json")))
    assert saved["meta"]["titulo"] == "Um Titulo"
    assert saved["meta"]["abertura"] == "Gancho de abertura."
    assert saved["cenas"][6]["origem"] == "p02q1"      # rastreabilidade

def test_coletor_falta_wav_erro(tmp="/tmp/_forma_c_falta"):
    shutil.rmtree(tmp, ignore_errors=True)
    src = _fake_assets(os.path.join(tmp, "src"))
    os.remove(os.path.join(src, "p01q3.wav"))          # remove uma narracao
    stage = os.path.join(tmp, "stage")
    try:
        forma_c.coletar_forma_c(ROT, src, stage)
        assert False, "deveria ter levantado erro de wav faltando"
    except FileNotFoundError as e:
        assert "p01q3" in str(e)

if __name__ == "__main__":
    test_coletor_encena_em_ordem()
    test_coletor_falta_wav_erro()
    print("OK coletor")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 skill/motioncomic/tests/test_forma_c.py`
Expected: ImportError/AttributeError (`forma_c` / `coletar_forma_c` não existe).

- [ ] **Step 3: Implement — `coletar_forma_c`**

Crie `skill/motioncomic/scripts/forma_c.py` com:

```python
"""Forma C — cola entre os assets do motioncomic (Forma A) e o skill
`diretor-animacao` (que dirige + renderiza via pixflow). NAO dirige nada aqui:
so encena os paineis textless + narracoes no layout que o diretor consome,
nomeia a saida, e envolve o filme dirigido com o card de gancho + o CTA.
"""
import json
import os
import shutil
import subprocess
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))
from build_motion import _title_clip, FPS, W, H   # noqa: E402  (card de abertura)
from build_travel import _cta_clip                # noqa: E402  (CTA inema.club)


def _paineis_em_ordem(roteiro):
    """Lista [(origem, tag)] na ordem de leitura: paginas em ordem, paineis 1..6.
    `origem`/`tag` = pNNqI (o nome do asset da Forma A)."""
    out = []
    for pg in roteiro["paginas"]:
        pn = pg["n"]
        for idx in range(1, len(pg["paineis"]) + 1):
            tag = f"p{pn:02d}q{idx}"
            out.append(tag)
    return out


def coletar_forma_c(roteiro, assets_dir, stage_dir):
    """Encena os paineis textless (assets/pNNqN.png) + narracoes (pNNqN.wav) da
    Forma A em `stage_dir/assets/{img,audio}/sNN.*` (na ORDEM de leitura), e
    escreve `stage_dir/forma-c.json` (cenas + meta). Retorna o manifesto dict.
    Levanta FileNotFoundError se faltar imagem ou narracao de algum painel."""
    img_dir = os.path.join(stage_dir, "assets", "img")
    aud_dir = os.path.join(stage_dir, "assets", "audio")
    os.makedirs(img_dir, exist_ok=True)
    os.makedirs(aud_dir, exist_ok=True)

    tags = _paineis_em_ordem(roteiro)
    cenas, imagens, narracoes = [], [], []
    # mapa origem -> prompt (dica de cena p/ o diretor VER)
    prompt_de = {}
    for pg in roteiro["paginas"]:
        for idx, p in enumerate(pg["paineis"], start=1):
            prompt_de[f"p{pg['n']:02d}q{idx}"] = p.get("prompt", "")

    for i, tag in enumerate(tags, start=1):
        src_png = os.path.join(assets_dir, f"{tag}.png")
        src_wav = os.path.join(assets_dir, f"{tag}.wav")
        if not os.path.exists(src_png):
            raise FileNotFoundError(f"painel sem imagem: {tag}.png em {assets_dir}")
        if not os.path.exists(src_wav):
            raise FileNotFoundError(f"painel sem narracao: {tag}.wav em {assets_dir}")
        sid = f"s{i:02d}"
        dst_png = os.path.join(img_dir, f"{sid}.png")
        dst_wav = os.path.join(aud_dir, f"{sid}.wav")
        shutil.copyfile(src_png, dst_png)
        shutil.copyfile(src_wav, dst_wav)
        imagens.append(dst_png)
        narracoes.append(dst_wav)
        cenas.append({"sid": sid, "origem": tag, "img": f"assets/img/{sid}.png",
                      "narr": f"assets/audio/{sid}.wav", "cena": prompt_de.get(tag, "")})

    meta = {"id": roteiro.get("id", ""), "n": roteiro.get("n"),
            "titulo": roteiro.get("titulo", ""), "subtitulo": roteiro.get("subtitulo", ""),
            "abertura": (roteiro.get("abertura") or "").strip()}
    manifesto = {"meta": meta, "cenas": cenas, "imagens": imagens, "narracoes": narracoes}
    with open(os.path.join(stage_dir, "forma-c.json"), "w") as f:
        json.dump(manifesto, f, ensure_ascii=False, indent=2)
    return manifesto
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 skill/motioncomic/tests/test_forma_c.py`
Expected: `OK coletor`

- [ ] **Step 5: Commit**

```bash
git add skill/motioncomic/scripts/forma_c.py skill/motioncomic/tests/test_forma_c.py
git commit -m "feat(motioncomic): Forma C coletor — encena paineis textless + narracoes p/ o diretor-animacao"
```

---

### Task 2: Naming `<serie>-c-epNN-<titulo>`

**Files:**
- Modify: `skill/motioncomic/scripts/forma_c.py` (acrescenta `forma_c_out_name`)
- Test: `skill/motioncomic/tests/test_forma_c.py` (acrescenta)

- [ ] **Step 1: Write the failing test**

Acrescente a `skill/motioncomic/tests/test_forma_c.py` (antes do `if __name__`, e registre no `__main__`):

```python
def test_naming_letra_c_antes_do_ep():
    nome = forma_c.forma_c_out_name("teo-e-o-guardiao-da-noite", 1, "A Noite do Susto")
    assert nome == "teo-e-o-guardiao-da-noite-c-ep01-a-noite-do-susto.mp4"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 skill/motioncomic/tests/test_forma_c.py`
Expected: AttributeError (`forma_c_out_name` não existe).

- [ ] **Step 3: Implement — `forma_c_out_name`**

Em `skill/motioncomic/scripts/forma_c.py`, acrescente o import do `ep_base` (do skill serie, via descoberta `_deps`) e a função. No topo, junto aos outros imports:

```python
import _deps  # noqa: E402
sys.path.insert(0, _deps.scripts("serie"))
from naming import ep_base   # noqa: E402  (mesmo gerador de nome das Formas A/B)
```

E a função (no corpo do módulo):

```python
def forma_c_out_name(serie_slug, n, titulo):
    """Nome do arquivo Forma C, no MESMO padrao das Formas A/B: a letra do
    formato (c) vem ANTES do epNN -> <serie>-c-ep01-<titulo>.mp4."""
    return ep_base(f"{serie_slug}-c", n, titulo) + ".mp4"
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 skill/motioncomic/tests/test_forma_c.py`
Expected: `OK coletor` + naming passa.
(Confirme que o import de `_deps`/`serie` não quebra o coletor — rode o arquivo inteiro.)

- [ ] **Step 5: Commit**

```bash
git add skill/motioncomic/scripts/forma_c.py skill/motioncomic/tests/test_forma_c.py
git commit -m "feat(motioncomic): Forma C naming <serie>-c-epNN (mesmo padrao de A/B)"
```

---

### Task 3: Wrapper — gancho (t=0) + miolo dirigido + CTA

**Files:**
- Modify: `skill/motioncomic/scripts/forma_c.py` (acrescenta `wrap_forma_c`)
- Test: `skill/motioncomic/tests/test_forma_c.py` (acrescenta)

- [ ] **Step 1: Write the failing test**

Acrescente a `skill/motioncomic/tests/test_forma_c.py` (antes do `if __name__`, e registre no `__main__`). Gera um "miolo" fake (clipe de cor 16:9 com áudio silencioso) e checa que o wrapper produz um MP4 mais longo (intro+miolo+cta) e válido:

```python
def _fake_clip(path, secs):
    import subprocess
    subprocess.run(["ffmpeg", "-nostdin", "-y", "-v", "error",
                    "-f", "lavfi", "-t", str(secs), "-i", f"color=c=black:s=1280x720:r=30",
                    "-f", "lavfi", "-t", str(secs),
                    "-i", "anullsrc=channel_layout=stereo:sample_rate=44100",
                    "-c:v", "libx264", "-pix_fmt", "yuv420p", "-r", "30",
                    "-c:a", "aac", "-ar", "44100", "-ac", "2", "-shortest", path], check=True)

def _dur(p):
    import subprocess
    return float(subprocess.run(["ffprobe", "-v", "error", "-show_entries",
        "format=duration", "-of", "default=nk=1:nw=1", p],
        capture_output=True, text=True).stdout.strip())

def test_wrapper_envolve_miolo(tmp="/tmp/_forma_c_wrap"):
    import shutil
    shutil.rmtree(tmp, ignore_errors=True); os.makedirs(tmp)
    miolo = os.path.join(tmp, "miolo.mp4"); _fake_clip(miolo, 4.0)
    out = os.path.join(tmp, "final.mp4")
    forma_c.wrap_forma_c(miolo, out, abertura="Gancho de teste.",
                         titulo="Titulo", subtitulo="sub", voice="bella")
    assert os.path.exists(out)
    # final > miolo (ganhou intro + cta)
    assert _dur(out) > _dur(miolo) + 1.0
    # tem video E audio
    import subprocess
    streams = subprocess.run(["ffprobe", "-v", "error", "-show_entries",
        "stream=codec_type", "-of", "default=nk=1:nw=1", out],
        capture_output=True, text=True).stdout
    assert "video" in streams and "audio" in streams
```

(Este teste roda TTS real via `_title_clip(audio=...)`/`_cta_clip` — precisa do `inemavox` em pé. Se o ambiente de teste não tiver TTS, ver Step 3 nota sobre injeção. Para a suíte do repo, este teste fica marcado como de integração — rodar quando os daemons estiverem de pé.)

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 skill/motioncomic/tests/test_forma_c.py`
Expected: AttributeError (`wrap_forma_c` não existe).

- [ ] **Step 3: Implement — `wrap_forma_c`**

Em `skill/motioncomic/scripts/forma_c.py`, acrescente:

```python
def wrap_forma_c(miolo_mp4, out_mp4, *, abertura, titulo, subtitulo="MOTION COMIC",
                 voice="bella"):
    """Envolve o filme dirigido (miolo) com o card de abertura narrando o gancho
    (t=0) e o CTA inema.club — mesma identidade das Formas A/B. Concatena via
    filtro `concat` (re-encoda), tolerante a diferenca de encode entre o pixflow
    e os cards. Retorna out_mp4."""
    import html as _h
    base = os.path.dirname(os.path.abspath(out_mp4))
    work = os.path.join(base, "_forma_c_cards")
    os.makedirs(work, exist_ok=True)
    abertura = (abertura or "").strip()

    intro = os.path.join(work, "000-intro.mp4")
    _title_clip(f'<small>{_h.escape(subtitulo or "MOTION COMIC")}</small>'
                + _h.escape(titulo), intro, secs=2.2,
                audio=(abertura or None), voice=voice)
    cta = os.path.join(work, "zz9-cta.mp4")
    _cta_clip(cta, voice=voice)

    parts = [intro, miolo_mp4, cta]
    # concat por FILTRO (re-encoda) -> tolera params diferentes entre pixflow e cards
    inputs = []
    for p in parts:
        inputs += ["-i", p]
    n = len(parts)
    fc = "".join(f"[{i}:v:0][{i}:a:0]" for i in range(n)) + f"concat=n={n}:v=1:a=1[v][a]"
    cmd = ["ffmpeg", "-nostdin", "-y", "-v", "error", *inputs,
           "-filter_complex", fc, "-map", "[v]", "-map", "[a]",
           "-c:v", "libx264", "-pix_fmt", "yuv420p", "-r", str(FPS),
           "-c:a", "aac", "-ar", "44100", "-ac", "2", out_mp4]
    subprocess.run(cmd, check=True)
    return out_mp4
```

- [ ] **Step 4: Run test to verify it passes**

Run (com `inemavox` em pé): `python3 skill/motioncomic/tests/test_forma_c.py`
Expected: `OK coletor` + naming + wrapper passam (o final é mais longo que o miolo e tem vídeo+áudio).

- [ ] **Step 5: Commit**

```bash
git add skill/motioncomic/scripts/forma_c.py skill/motioncomic/tests/test_forma_c.py
git commit -m "feat(motioncomic): Forma C wrapper — gancho(t=0) + miolo dirigido + CTA via concat-filtro"
```

---

### Task 4: Teo ep1 Forma C ponta a ponta (PROCEDIMENTO dirigido — manual/Claude)

Não é TDD: é executar o fluxo real (o diretor-animacao é dirigido por Claude). Produz e valida o 1º filme.

**Files:** saída em `~/projetos/output/teo-guardiao-da-noite/`.

- [ ] **Step 1: Garantir os assets da Forma A do ep1**

Os painéis textless + narrações do Teo ep1 já existem do render da Forma A, em
`~/projetos/output/teo-guardiao-da-noite/_work/<base-a-ep01>/assets/p*q*.{png,wav}`.
Run (confirmar):
```bash
ls ~/projetos/output/teo-guardiao-da-noite/_work/*a-ep01*/assets/p*q*.png | wc -l
ls ~/projetos/output/teo-guardiao-da-noite/_work/*a-ep01*/assets/p*q*.wav | wc -l
```
Expected: 12 e 12. Se faltar, rodar a Forma A do ep1 antes (gera os assets).

- [ ] **Step 2: Encenar p/ o diretor (coletor)**

Run:
```bash
cd /home/nmaldaner/projetos/inemaref && python3 - <<'PY'
import sys, json, glob; sys.path.insert(0, "skill/motioncomic/scripts")
import forma_c
rot = json.load(open("/home/nmaldaner/projetos/output/teo-guardiao-da-noite/fonte/ep01.json"))
rot["subtitulo"] = "as invencoes do Teo"
assets = glob.glob("/home/nmaldaner/projetos/output/teo-guardiao-da-noite/_work/*a-ep01*/assets")[0]
stage = "/home/nmaldaner/projetos/output/teo-guardiao-da-noite/_formaC/ep01"
man = forma_c.coletar_forma_c(rot, assets, stage)
print("encenado:", len(man["imagens"]), "imagens em", stage)
PY
```
Expected: `encenado: 12 imagens em .../_formaC/ep01` + `forma-c.json` escrito.

- [ ] **Step 3: Dirigir + renderizar o MIOLO (invocar o skill diretor-animacao)**

Invoque o skill `diretor-animacao` apontando para o material encenado
(`.../_formaC/ep01/assets/img/sNN.png` + `.../assets/audio/sNN.wav`), pedindo o filme
**direto** (sem portão de decupagem). O diretor: VÊ cada imagem (desenho ⇒ `parallax 0`),
decide câmera por quadro, monta a trilha (`montar-trilha.mjs`), escreve o spec pixflow e
renderiza. Saída esperada: um `miolo.mp4` (16:9) na pasta de trabalho do diretor.
Anote o caminho do `miolo.mp4` produzido.

- [ ] **Step 4: Envolver com gancho + CTA (wrapper) e nomear**

Run (ajuste `MIOLO` para o caminho do Step 3):
```bash
cd /home/nmaldaner/projetos/inemaref && python3 - <<'PY'
import sys, json; sys.path.insert(0, "skill/motioncomic/scripts")
import forma_c
MIOLO = "/CAMINHO/DO/miolo.mp4"   # <- do Step 3
out_dir = "/home/nmaldaner/projetos/output/teo-guardiao-da-noite"
nome = forma_c.forma_c_out_name("teo-e-o-guardiao-da-noite", 1, "A Noite do Susto")
out = f"{out_dir}/{nome}"
rot = json.load(open(f"{out_dir}/fonte/ep01.json"))
forma_c.wrap_forma_c(MIOLO, out, abertura=rot.get("abertura",""),
                     titulo=rot["titulo"], subtitulo="as invencoes do Teo", voice="bella")
print("Forma C:", out)
PY
```
Expected: cria `teo-e-o-guardiao-da-noite-c-ep01-a-noite-do-susto.mp4` na pasta da série.

- [ ] **Step 5: Verificar**

Run:
```bash
cd ~/projetos/output/teo-guardiao-da-noite
M=teo-e-o-guardiao-da-noite-c-ep01-a-noite-do-susto.mp4
ffprobe -v error -show_entries format=duration -of default=nk=1:nw=1 "$M"
ffmpeg -nostdin -v error -y -i "$M" -vf fps=1/6 /tmp/teo_c_ep1/f%02d.png
```
Conferir (visão): o **card de abertura** abre com o gancho (t=0); o **miolo** mostra os painéis com câmera dirigida (movimento variado, não push-in uniforme), transições e look; o **CTA** fecha. Brilho estável nas fronteiras (anti-pisca — receita nº 1 do `DICAS-CORRECOES.md` do diretor). Se houver pisca/erro de direção, ajustar com o diretor e re-renderizar o miolo (Step 3), depois re-empacotar (Step 4).

- [ ] **Step 6: Sem commit de código** (saída é dado, fica no output, fora do git).

---

### Task 5: Teo eps 2–5 Forma C (PROCEDIMENTO — direto)

**Files:** saída em `~/projetos/output/teo-guardiao-da-noite/`.

- [ ] **Step 1: Repetir Task 4 (Steps 1–5) para os eps 2..5**

Para cada `n` em 2..5: confirmar assets da Forma A → coletor (`epNN`, stage `_formaC/epNN`) →
invocar `diretor-animacao` **direto** no material encenado → `wrap_forma_c` com o gancho/título
do episódio → nome `forma_c_out_name("teo-e-o-guardiao-da-noite", n, <titulo>)` → verificar.
Títulos: ep02 "Os Olhos do Escuro", ep03 "A Bancada do Teo", ep04 "O Guardião Acorda",
ep05 "Amigos da Noite".

- [ ] **Step 2: Conferir a pasta final (A, B e C lado a lado)**

Run:
```bash
ls ~/projetos/output/teo-guardiao-da-noite/teo-e-o-guardiao-da-noite-{a,b,c}-ep0*.mp4 | sort
```
Expected: 15 arquivos (5 por formato), nomes `-a-`/`-b-`/`-c-` na mesma pasta.

- [ ] **Step 3: Sem commit de código.**

---

## Self-Review

**Spec coverage:**
- Motor = diretor-animacao (Claude-driven) → Tasks 3/4/5 (invocação) + Task 0 (viabilidade). ✓
- Reusa painéis textless + narração da Forma A, sem regerar → Task 1 (coletor copia assets existentes). ✓
- parallax 0 (desenho) → instrução ao diretor na Task 4 Step 3. ✓
- Roda direto (sem portão) → Task 4 Step 3 / Task 5. ✓
- Mantém gancho (t=0) + CTA → Task 3 (`wrap_forma_c` reusa `_title_clip` com áudio + `_cta_clip`). ✓
- Saída `<serie>-c-epNN-<titulo>.mp4` na mesma pasta → Task 2 (naming) + Task 4 Step 4. ✓
- Forma C não é tipo do build_serie → nenhuma task toca build_serie. ✓
- Validar render antes do resto → Task 0 (portão). ✓

**Placeholder scan:** sem TBD/TODO. O único `/CAMINHO/DO/miolo.mp4` (Task 4 Step 4) é um valor que SÓ existe após o Step 3 (a saída do diretor) — está explicitamente marcado como "<- do Step 3", não é placeholder de implementação.

**Type consistency:** `coletar_forma_c(roteiro, assets_dir, stage_dir) -> manifesto{meta,cenas,imagens,narracoes}` usado consistentemente; `forma_c_out_name(serie_slug, n, titulo) -> "<...>.mp4"`; `wrap_forma_c(miolo_mp4, out_mp4, *, abertura, titulo, subtitulo, voice)`. `_title_clip(text, out, secs, audio, voice)` e `_cta_clip(out, voice)` batem as assinaturas reais do motioncomic. `ep_base(serie_slug, n, titulo)` é o mesmo do `serie/naming.py` usado por A/B.

## Pré-requisitos de execução

- **Task 0/3/4/5:** `pixflow` + `diretor-animacao` em `~/projetos`, GPU, node/remotion/chromium; `ffmpeg`; `inemavox` (TTS, p/ o gancho/CTA e Task 3); `inemaimg` só se precisar (re)gerar assets da Forma A.
- **Tasks 1/2:** só Python+ffmpeg (testes usam assets fake; Task 3 precisa de TTS real).
