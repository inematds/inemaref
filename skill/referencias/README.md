# referencias — núcleo comum (a construir)

Compartilhado entre as skills `folder` e `quadrinho`:

- **estilos** — presets foto-realista e cartoon/ilustrado.
- **consistência** — regra de travamento da pessoa real (face ref / LoRA), definida após o experimento (`../../docs/04`).
- **mapa de quadros** — layout de prancha (coordenadas + ordem de leitura) que alimenta o motion comic.
